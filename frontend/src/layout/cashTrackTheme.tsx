import { extendTheme } from "@chakra-ui/react";

const cashTrackTheme = {
    colors: {
        cash: {
            bg: "#2C3E50",
            green: "#1ABC9C",
            white: "#ECF0F1"
        }
    }
}
export default extendTheme(cashTrackTheme);